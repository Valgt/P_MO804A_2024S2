{"uid": "338-E", "url": "https://codeforces.com/contest/338/problem/E", "tags": ["data structures"], "title": "338-E", "statement": "E. Optimize!time limit per test2 secondsmemory limit per test256 megabytesinputstdinoutputstdoutManao is solving a problem with the following statement:He came up with a solution that produces the correct answers but is too slow. You are given the pseudocode of his solution, where the function getAnswer calculates the answer to the problem:getAnswer(a[1..n], b[1..len], h)answer = 0for i = 1 to n-len+1answer = answer + f(a[i..i+len-1], b, h, 1)return answerf(s[1..len], b[1..len], h, index)if index = len+1 thenreturn 1for i = 1 to lenif s[index] + b[i] >= hmem = b[i]b[i] = 0res = f(s, b, h, index + 1)b[i] = memif res > 0return 1return 0Your task is to help Manao optimize his algorithm.InputThe first line contains space-separated integersn,lenandh(1 ≤len≤n≤ 150000; 1 ≤h≤ 109). The second line containslenspace-separated integersb1,b2, ...,blen(1 ≤bi≤ 109). The third line containsnspace-separated integersa1,a2, ...,an(1 ≤ai≤ 109).OutputPrint a single number — the answer to Manao's problem.ExamplesInput5 2 105 31 8 5 5 7Output2", "source": "CF", "vjudge": false, "processed": [{"prompt_md5": "0ee889b7", "template_md5": "35d339e4", "result": "You are given three arrays:\n\n*  $a$ of length $n$\n*  $b$ of length $len$\n*  $h$ is an integer.\n\nYou need to find the number of times you can perform the following operation:\n\n1. Choose a subarray $s$ of $a$ with length $len$.\n2. For each element $b_i$ in $b$, if $s[index] + b_i \\ge h$, then set $b_i$ to 0.\n3. If after step 2, all elements in $b$ are 0, return 1. Otherwise, return 0.\n\nThe operation can be performed for all possible subarrays $s$ of $a$.\n\n**Input:**\n\n* The first line contains three space-separated integers: $n$, $len$, and $h$.\n* The second line contains $len$ space-separated integers: $b_1$, $b_2$, ..., $b_{len}$.\n* The third line contains $n$ space-separated integers: $a_1$, $a_2$, ..., $a_n$.\n\n**Output:**\n\nPrint a single integer: the number of times the operation can be performed successfully."}, {"prompt_md5": "52b8a43c", "template_md5": "465699a0", "result": "You are given two arrays, `a` and `b`, and a number `h`.  You need to find the number of times you can combine a subarray of length `len` from `a` with elements from `b` to get a sum greater than or equal to `h`."}]}